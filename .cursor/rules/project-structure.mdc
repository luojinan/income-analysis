# Project Structure Guide

This project is a Vite-powered React application using TypeScript.

- The main entry point is [src/main.tsx](mdc:src/main.tsx), which mounts the root React component ([src/App.tsx](mdc:src/App.tsx)) into the DOM.
- Static assets are located in [public/](mdc:public/) and [src/assets/](mdc:src/assets/).
- Global styles are in [src/index.css](mdc:src/index.css) and [src/App.css](mdc:src/App.css).
- Project configuration files include [vite.config.ts](mdc:vite.config.ts), [tsconfig.json](mdc:tsconfig.json), and [package.json](mdc:package.json).
- The project uses [pnpm](mdc:pnpm-lock.yaml) for package management.

# Project Structure Guide

This project is a Vite-powered React application using TypeScript.

- The main entry point is [src/main.tsx](mdc:src/main.tsx), which mounts the root React component ([src/App.tsx](mdc:src/App.tsx)) into the DOM.
- Static assets are located in [public/](mdc:public/) and [src/assets/](mdc:src/assets/).
- Global styles are in [src/index.css](mdc:src/index.css) and [src/App.css](mdc:src/App.css).
- Project configuration files include [vite.config.ts](mdc:vite.config.ts), [tsconfig.json](mdc:tsconfig.json), and [package.json](mdc:package.json).
- The project uses [pnpm](mdc:pnpm-lock.yaml) for package management.

---
